import { colors } from '../constants';

export const GameEndScoreCSS = `
  .GameEndScore {
    color: ${colors.green};
    font-family: 'Francois One', sans-serif;
    font-size: 1.4rem;
    letter-spacing: .1em;
    text-align: center;
  }
`;

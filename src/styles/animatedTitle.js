export const AnimatedTitleCSS = ` 
  .AnimatedTitle {
    align-items: center;
    display: flex;
    justify-content: center;
    width: 100%; /* If the width isn't setted, the SVG won't scale */
  }

  /* Put some space between the letters */
  svg {
    margin-left: 2vmin;
    top: 10vmin;
  }
`;
